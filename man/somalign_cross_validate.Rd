% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{somalign_cross_validate}
\alias{somalign_cross_validate}
\title{Cross-validate label transfer on held-out cells}
\usage{
somalign_cross_validate(
  data,
  labels,
  grid,
  k = 5L,
  stratify = TRUE,
  epsilon = 0.1,
  solver = "internal",
  rlen = 20L,
  n_bins = 10L,
  seed = 1L,
  ...
)
}
\arguments{
\item{data}{Numeric cell-by-feature matrix.}

\item{labels}{Character vector of per-cell labels, \code{nrow(data)} long.}

\item{grid}{A \code{kohonen::somgrid} for the reference and query SOMs.}

\item{k}{Positive integer. Number of folds. Default \code{5L}.}

\item{stratify}{Logical. Stratify folds by class. Default \code{TRUE}.}

\item{epsilon, solver}{Passed to \code{\link[=somalign_fit]{somalign_fit()}}.}

\item{rlen}{SOM training iterations for both SOMs. Default \code{20L}.}

\item{n_bins}{Calibration bins. Default \code{10L}.}

\item{seed}{Integer or \code{NULL}. RNG seed, restored on exit. Default \code{1L}.}

\item{...}{Further arguments forwarded to \code{\link[=somalign_fit]{somalign_fit()}}.}
}
\value{
A list of class \code{somalign_cross_validation} with \code{metrics}
(\code{\link[=somalign_label_metrics]{somalign_label_metrics()}}), \code{calibration} (\code{\link[=somalign_calibration]{somalign_calibration()}}),
\code{per_fold} (data frame), \code{predictions} (pooled per-cell data frame), \code{k}.
}
\description{
Stratified k-fold cross-validation that measures how accurately somalign
transfers labels to cells it has not seen. Each fold trains a reference SOM
on the training split, projects the held-out split, and transfers labels;
every held-out cell has a real ground-truth label, so this estimates true
generalisation without any external labelled query data. Pooled results are
scored with \code{\link[=somalign_label_metrics]{somalign_label_metrics()}} and \code{\link[=somalign_calibration]{somalign_calibration()}}.
}
\examples{
\donttest{
if (requireNamespace("kohonen", quietly = TRUE)) {
  set.seed(1)
  x <- rbind(matrix(rnorm(200, -2), ncol = 2), matrix(rnorm(200, 2), ncol = 2))
  colnames(x) <- c("f1", "f2")
  lab <- rep(c("low", "high"), each = 100)
  cv <- somalign_cross_validate(x, lab,
    grid = kohonen::somgrid(2, 2, "hexagonal"), k = 3)
  cv$metrics$accuracy
}
}
}
