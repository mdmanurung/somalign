% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{somalign_epsilon_sweep}
\alias{somalign_epsilon_sweep}
\title{Epsilon phase-transition sweep for principled epsilon selection}
\usage{
somalign_epsilon_sweep(
  query,
  reference,
  epsilon_grid = NULL,
  n_grid = 25L,
  rho_query = 1,
  rho_ref = 1,
  solver = c("log_domain", "internal", "auto", "annealing"),
  max_iter = 1000,
  tol = 1e-07,
  diagonal_boost = 0,
  label_guided = FALSE,
  parallel = FALSE,
  anneal_start = 10,
  anneal_factor = NULL,
  anneal_stages = 10L
)
}
\arguments{
\item{query}{A \code{somalign_query} object.}

\item{reference}{A \code{somalign_reference} object.}

\item{epsilon_grid}{Numeric vector of epsilon values. If \code{NULL} (default),
a log-spaced grid of \code{n_grid} values from 1e-3 to 5 is used.}

\item{n_grid}{Integer. Grid size when \code{epsilon_grid = NULL}. Default \code{25}.}

\item{rho_query, rho_ref}{Mass relaxation parameters passed to the OT solver.}

\item{solver}{Sinkhorn solver. Default \code{"log_domain"} (required for
\code{log_Z}; \code{"internal"}/\code{"auto"} fill \code{log_Z} with \code{NA}). \code{"annealing"}
runs the geometric epsilon-cooling schedule at each grid point (using
\code{anneal_start}/\code{anneal_factor}/\code{anneal_stages}) and also reports \code{log_Z}.}

\item{max_iter, tol}{Sinkhorn convergence parameters.}

\item{diagonal_boost}{Non-negative cost reduction on nearest-reference-node
entries. Default \code{0}.}

\item{label_guided}{Logical; see \code{\link[=somalign_fit]{somalign_fit()}}.}

\item{parallel}{Logical; see \code{\link[=somalign_sensitivity_grid]{somalign_sensitivity_grid()}}.}

\item{anneal_start, anneal_factor, anneal_stages}{Annealing-schedule tuning
parameters, used only when \code{solver = "annealing"}. See \code{\link[=somalign_fit]{somalign_fit()}}.}
}
\value{
A list of class \code{"somalign_epsilon_sweep"} with components
\code{table}, \code{epsilon_c}, \code{epsilon_rec}, \code{cost_scale}. The \code{table} data
frame has one row per epsilon value with columns \code{epsilon},
\code{log_epsilon}, \code{Phi}, \code{susceptibility}, \code{log_Z}, \code{mutual_information},
\code{conditional_entropy_mean}, \code{expected_cost}, \code{transport_mass},
\code{iterations}, \code{converged}.
}
\description{
Runs the Sinkhorn OT solve across a log-spaced epsilon grid without the
per-cell projection step, computing the transport-plan order parameter
(mean fractional reference-node usage, a perplexity-based measure), its
susceptibility (rate of change with log epsilon), the dual free energy,
and the mutual information between query and reference nodes at each
grid point.
}
\details{
The order parameter \eqn{\Phi(\epsilon)} is the mean effective fraction of
reference nodes used per query node: \eqn{\Phi = \mathrm{mean}_i(2^{H_i}) /
K}, where \eqn{H_i} is the (bit) conditional entropy of the row-normalised
transport plan for query node \eqn{i} and \eqn{K} is the number of
reference nodes. As \eqn{\epsilon \to 0}, \eqn{\Phi \to 1/K}; as
\eqn{\epsilon \to \infty}, \eqn{\Phi \to 1}. The susceptibility
\eqn{\chi = d\Phi/d\log\epsilon} peaks at the critical epsilon
\eqn{\epsilon_c}, marking the crossover between a localised
(transport-cost-dominated) and delocalised (entropy-dominated) plan.
\code{epsilon_rec} (\eqn{0.3 \, \epsilon_c}) keeps the plan in the ordered phase
with a safety margin.

The sweep avoids the per-cell projection step, so it runs approximately
one OT solve per epsilon value -- much faster than
\code{\link[=somalign_sensitivity_grid]{somalign_sensitivity_grid()}} for the same epsilon range.
}
\examples{
set.seed(1)
mat <- matrix(rnorm(40), nrow = 20, ncol = 2,
              dimnames = list(NULL, c("F1", "F2")))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
qry <- somalign_query(mat + 0.5, ref,
                      grid = kohonen::somgrid(2, 2, "hexagonal"), rlen = 5)
sw <- somalign_epsilon_sweep(qry, ref, n_grid = 8)
sw$epsilon_c
sw$epsilon_rec
}
\seealso{
\code{\link[=somalign_fit]{somalign_fit()}}, \code{\link[=somalign_select_epsilon]{somalign_select_epsilon()}},
\code{\link[=somalign_sensitivity_grid]{somalign_sensitivity_grid()}}, \code{\link[=somalign_diagnostics]{somalign_diagnostics()}}
}
