% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference.R
\name{somalign_reference}
\alias{somalign_reference}
\title{Build a reference object from an existing SOM and old data}
\usage{
somalign_reference(
  som_ref,
  data,
  labels = NULL,
  features = NULL,
  center = NULL,
  scale = NULL,
  codebook_space = NULL,
  quantile_probs = c(0.5, 0.9, 0.95, 0.99)
)
}
\arguments{
\item{som_ref}{A \code{kohonen} SOM object, or a SOM-like object containing a
codebook matrix.}

\item{data}{Numeric old/reference data used to compute scaling, masses,
labels, and distance thresholds.}

\item{labels}{Optional labels, one per row of \code{data}.}

\item{features}{Optional feature names to use. Defaults to all columns.}

\item{center}{Optional saved feature centers. Computed from \code{data} when
omitted.}

\item{scale}{Optional saved feature scales. Computed from \code{data} when
omitted.}

\item{codebook_space}{Coordinate system of the existing \code{som_ref} codebook.
Use \code{"reference_scaled"} when the SOM was trained on \code{data} transformed
with \code{center} and \code{scale}; use \code{"raw"} when the SOM was trained on raw
feature values and should be transformed into reference-scaled space.}

\item{quantile_probs}{Distance quantiles used for outside-reference flags.}
}
\value{
A \code{somalign_reference} object.
}
\description{
Build a reference object from an existing SOM and old data
}
\examples{
\dontrun{
set.seed(1)
mat <- matrix(rnorm(20), nrow = 10, ncol = 2,
              dimnames = list(NULL, c("F1", "F2")))
g <- kohonen::somgrid(2, 2, "hexagonal")
som_obj <- kohonen::som(scale(mat), grid = g, rlen = 10)
ref <- somalign_reference(som_obj, mat, labels = rep(c("A", "B"), each = 5),
                          codebook_space = "reference_scaled")
}
}
