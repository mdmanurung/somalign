% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{somalign_plot_marker_distributions}
\alias{somalign_plot_marker_distributions}
\title{Plot per-marker cell distributions before projection}
\usage{
somalign_plot_marker_distributions(
  query,
  reference = NULL,
  reference_data = NULL,
  features = NULL,
  downsample = 2000L,
  seed = 1L
)
}
\arguments{
\item{query}{A \code{somalign_query} object.}

\item{reference}{Optional \code{somalign_reference} object. When supplied, its
SOM codebook values are shown as a rug of node prototypes.}

\item{reference_data}{Optional numeric matrix of reference cells in
reference-scaled space (cells x features). When supplied, a second
density curve is shown instead of the codebook rug. Takes precedence over
\code{reference}.}

\item{features}{Character vector of features to plot. \code{NULL} (default) uses
all features in \code{query}.}

\item{downsample}{Maximum number of cells to subsample from \code{query} (and
\code{reference_data} when supplied) for plotting speed. Default \code{2000}.}

\item{seed}{Integer seed for the random subsample. Default \code{1}.}
}
\value{
A \code{ggplot} object.
}
\description{
Density plot of query cells (reference-scaled, downsampled) faceted by
marker. When a \code{somalign_reference} object is supplied via \code{reference}, the
reference SOM code values for that marker are overlaid as a rug of node
prototypes (red tick marks). When raw reference cell data are available,
pass them as \code{reference_data} for a true cell-vs-cell density comparison.
}
\examples{
set.seed(1)
mat <- matrix(rnorm(20), nrow = 10, ncol = 2,
              dimnames = list(NULL, c("F1", "F2")))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
qry <- somalign_query(mat, ref, grid = kohonen::somgrid(2, 2, "hexagonal"),
                      rlen = 5)
somalign_plot_marker_distributions(qry, reference = ref)
}
