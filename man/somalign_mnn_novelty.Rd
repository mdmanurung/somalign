% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mnn-novelty.R
\name{somalign_mnn_novelty}
\alias{somalign_mnn_novelty}
\title{Mutual-nearest-neighbour novelty flag for query SOM nodes}
\usage{
somalign_mnn_novelty(fit)
}
\arguments{
\item{fit}{A \code{somalign_fit} object.}
}
\value{
A named logical vector of length equal to the number of query SOM
nodes.  \code{TRUE} indicates that node is MNN-unmatched with the reference
(no reciprocal k=1 nearest neighbour) and is therefore a candidate novel
node.  Names are \code{"q1"}, \code{"q2"}, ... matching query node indices.
}
\description{
Computes a per-query-node "unmatched" flag based on mutual nearest-neighbour
(MNN) reciprocity between the query and reference codebooks.  This is a
\strong{supplementary signal} intended for experimental evaluation (Experiment E2)
of whether MNN non-reciprocity adds novelty-detection value over the
unbalanced-OT leftover-mass signal (\code{match_fraction} / \code{diagnostics$nodes}).
It does \strong{not} replace or modify the OT engine; the full Sinkhorn coupling,
soft-label posterior, and barycentric correction are unaffected.
}
\section{How reciprocity is computed}{

Let \code{Q} be the query codebook and \code{R} the reference codebook, both in
\strong{reference-scaled space} (the same coordinate system used to build the OT
cost matrix).  For each query node \code{q}, its nearest reference node is
\verb{r(q) = argmin_j ||Q[q,] - R[j,]||}.  For each reference node \code{r}, its
nearest query node is \verb{q(r) = argmin_i ||R[r,] - Q[i,]||}.  Query node \code{q}
is flagged \code{mnn_unmatched = TRUE} if and only if \code{q(r(q)) != q} — the
back-projection does not return to the originating query node.
}

\section{Batch-shift confound (critical for E2)}{

The flag is computed on the \strong{pre-correction} (raw) query codebook.  Under a
population-specific batch shift, query nodes that represent populations already
present in the reference can break reciprocity purely because their codebook
vectors are displaced relative to the reference, even though no true novelty
exists.  This false-fire rate rises with shift magnitude and constitutes a
systematic confound that Experiment E2 is specifically designed to measure.
The OT-derived \code{match_fraction} is not confounded in the same way because the
Sinkhorn plan transports mass globally across the cost matrix.
}

\section{Feature weights}{

When the OT fit used \code{feature_weights} (diagonal Mahalanobis cost), the OT
cost lives in \code{sqrt(w)}-scaled space, whereas this function always operates on
unweighted reference-scaled coordinates.  If you want exact alignment with the
OT cost space, apply \code{sqrt(feature_weights)} column scaling to both codebooks
before calling this function manually via \code{.somalign_nearest_code()}.  For the
E2 evaluation setting (unweighted default fits) this discrepancy is absent.
}

\examples{
set.seed(42)
mat <- matrix(rnorm(100), nrow = 50, ncol = 2,
              dimnames = list(NULL, c("F1", "F2")))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
qry <- somalign_query(mat, ref, grid = kohonen::somgrid(2, 2, "hexagonal"),
                      rlen = 5)
fit <- somalign_fit(qry, ref)
flags <- somalign_mnn_novelty(fit)
}
\seealso{
\code{\link[=somalign_mnn_novelty_cells]{somalign_mnn_novelty_cells()}} to broadcast the per-node flag to
individual cells; \code{fit$diagnostics$nodes$match_fraction} for the
OT-derived leftover-mass signal.
}
