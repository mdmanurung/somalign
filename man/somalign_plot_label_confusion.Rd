% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{somalign_plot_label_confusion}
\alias{somalign_plot_label_confusion}
\title{Plot label transfer confusion heatmap}
\usage{
somalign_plot_label_confusion(fit, min_confidence = NULL)
}
\arguments{
\item{fit}{A \code{somalign_fit} object.}

\item{min_confidence}{Minimum \code{transferred_label_confidence} to include.
\code{NULL} (default) imposes no additional filter beyond acceptance.}
}
\value{
A \code{ggplot} object.
}
\description{
Row-normalised heatmap of old-to-transferred label pairs for accepted
cells. High values on the diagonal indicate coherent transfer; strong
off-diagonal entries warrant further inspection.
}
\examples{
set.seed(1)
mat <- matrix(rnorm(20), nrow = 10, ncol = 2,
              dimnames = list(NULL, c("F1", "F2")))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
qry <- somalign_query(mat, ref, grid = kohonen::somgrid(2, 2, "hexagonal"),
                      rlen = 5)
fit <- somalign_fit(qry, ref)
somalign_plot_label_confusion(fit)
}
