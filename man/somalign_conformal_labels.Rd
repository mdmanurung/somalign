% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conformal.R
\name{somalign_conformal_labels}
\alias{somalign_conformal_labels}
\title{Conformal prediction sets for label transfer}
\usage{
somalign_conformal_labels(
  prob_query,
  prob_calibration,
  truth_calibration,
  alpha = 0.1,
  class_conditional = FALSE
)
}
\arguments{
\item{prob_query}{Numeric matrix, query cells by classes, of per-cell class
probabilities (columns named by class). Rows need not sum to one.}

\item{prob_calibration}{Numeric matrix, calibration cells by classes, with the
same class columns as \code{prob_query}.}

\item{truth_calibration}{Character/factor vector of true classes for the
calibration cells; every value must be a column of \code{prob_calibration}.}

\item{alpha}{Target miscoverage in (0, 1); coverage is at least \code{1 - alpha}.
Default \code{0.1}.}

\item{class_conditional}{Logical; if \code{TRUE}, calibrate a per-class threshold
(Mondrian conformal) for class-conditional coverage. Default \code{FALSE}.}
}
\value{
An object of class \code{somalign_conformal}: a list with \code{sets} (a
query-by-class logical matrix; \code{TRUE} = class is in the cell's set),
\code{set_size} (per-cell integer), \code{abstain} (per-cell logical, \code{set_size == 0}),
\code{threshold} (the calibrated \code{q}, scalar or per-class), \code{alpha}, \code{classes},
and \code{class_conditional}.
}
\description{
Turns per-cell class probabilities into \strong{conformal prediction sets} with a
distribution-free, finite-sample coverage guarantee. Given a labelled
calibration set, \code{somalign_conformal_labels()} calibrates a single threshold
(split conformal) so that, for a fresh exchangeable query cell, the true label
is contained in the returned set with probability at least \code{1 - alpha}. A cell
whose set is empty is an \strong{abstention}; a set of size one is a confident
assignment; a larger set flags genuine ambiguity between labels.
}
\details{
This complements the heuristic acceptance gate of \code{\link[=somalign_results]{somalign_results()}}: rather
than thresholding a single confidence, it returns calibrated label \emph{sets} with
an explicit coverage level. The probability matrices are typically the per-cell
soft label memberships from \code{\link[=somalign_soft_labels]{somalign_soft_labels()}}, but any cells-by-classes
matrix of non-negative scores works.

The nonconformity score is \verb{1 - p(true class)}; the set for a query cell is
every class whose probability is at least \code{1 - q}, where \code{q} is the
\code{ceiling((n + 1)(1 - alpha)) / n} empirical quantile of the calibration scores
(Angelopoulos & Bates, 2023, \doi{10.1561/2200000101}). With
\code{class_conditional = TRUE} the threshold is computed separately per true class
(Mondrian conformal), giving class-conditional rather than only marginal
coverage, which is preferable under class imbalance.
}
\examples{
set.seed(1)
classes <- c("A", "B", "C")
# calibrated toy probabilities: true class gets the most mass on average
gen <- function(n) {
  y <- sample(classes, n, replace = TRUE)
  p <- matrix(stats::runif(n * 3, 0, 0.4), n, 3, dimnames = list(NULL, classes))
  p[cbind(seq_len(n), match(y, classes))] <-
    p[cbind(seq_len(n), match(y, classes))] + 0.6
  list(p = p / rowSums(p), y = y)
}
cal <- gen(500); qry <- gen(200)
cp <- somalign_conformal_labels(qry$p, cal$p, cal$y, alpha = 0.1)
cp
}
