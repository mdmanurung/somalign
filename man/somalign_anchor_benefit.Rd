% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{somalign_anchor_benefit}
\alias{somalign_anchor_benefit}
\title{Quantify the label-transfer benefit of anchor (repeat) samples}
\usage{
somalign_anchor_benefit(
  query,
  reference,
  query_labels,
  anchor_old,
  anchor_new,
  rho_grid = c(0, 0.5, 1, 2, 5),
  metric = c("mcc", "macro_f1", "accuracy"),
  eval_mask = NULL,
  epsilon = 0.1,
  rho_query = 1,
  rho_ref = 1,
  min_match_fraction = 0.05,
  confidence_threshold = 0.6,
  solver = "internal",
  max_iter = 1000,
  tol = 1e-07,
  chunk_size = 10000L,
  n_bins = 10L
)
}
\arguments{
\item{query}{A \code{somalign_query} object.}

\item{reference}{A labelled \code{somalign_reference} object.}

\item{query_labels}{Character ground-truth labels, one per query cell.}

\item{anchor_old, anchor_new}{Paired anchor cell matrices (repeat samples in
the reference and query batches respectively), same number of rows.}

\item{rho_grid}{Non-negative anchor strengths to sweep. Default
\code{c(0, 0.5, 1, 2, 5)}; include \code{0} for the no-anchor baseline.}

\item{metric}{Objective for \code{best}/\code{lift}: \code{"mcc"} (default), \code{"macro_f1"},
or \code{"accuracy"} (all maximised).}

\item{eval_mask}{Optional logical vector, one per query cell, selecting the
cells to score (e.g. exclude the anchor samples for a clean held-out
measure). Default \code{NULL} scores all cells.}

\item{epsilon, rho_query, rho_ref, solver, max_iter, tol}{OT settings.}

\item{min_match_fraction, confidence_threshold}{Label-acceptance gates.}

\item{chunk_size}{Anchor-projection chunk size.}

\item{n_bins}{Calibration bins for the \code{ece} column.}
}
\value{
A list of class \code{somalign_anchor_benefit} with \code{grid} (one row per
\code{rho_anchor}: accuracy, macro_f1, mcc, coverage, ece), \code{baseline} (the
\code{rho_anchor = 0} row), \code{best}, \code{lift} (best minus baseline on \code{metric}),
and \code{metric}.
}
\description{
Measures how much anchor samples -- QC/repeat specimens run in both the
reference and query batches -- improve label transfer, by sweeping the anchor
cost-bonus strength \code{rho_anchor} and scoring the transferred labels against a
\emph{known} query label. \code{rho_anchor = 0} is the exact no-anchor baseline
(plain \code{\link[=somalign_fit]{somalign_fit()}}); positive values bias the transport plan toward
anchor-supported node pairs, exactly as \code{somalign_fit_anchored(correction = "cost_bonus")}. Anchors influence labels only through this cost bonus, so
this single sweep captures their entire label-transfer effect (the
\code{"subspace"} correction leaves labels identical to \code{rho_anchor = 0}).
}
\details{
This is a \emph{validation} tool: it needs ground-truth query labels (e.g. an
independently gated held-out batch), which production label transfer does
not have. It reuses the fixed reference/query SOMs and a single anchor count
matrix across the sweep, recomputing only the OT solve, so it is cheap.
}
\seealso{
\code{\link[=somalign_fit_anchored]{somalign_fit_anchored()}}, \code{\link[=somalign_cross_validate]{somalign_cross_validate()}}
}
