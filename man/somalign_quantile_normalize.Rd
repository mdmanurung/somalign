% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{somalign_quantile_normalize}
\alias{somalign_quantile_normalize}
\title{Divide each feature column by its upper quantile}
\usage{
somalign_quantile_normalize(data, reference, probs = 0.999, features = NULL)
}
\arguments{
\item{data}{Numeric matrix of query data, same format as the \code{data} argument
to \code{\link[=somalign_query]{somalign_query()}}.}

\item{reference}{A \code{somalign_reference} object.}

\item{probs}{Single numeric in the open interval (0, 1). The quantile used
as the normalisation denominator. Default is 0.999 (99.9th percentile).}

\item{features}{Optional character vector of feature names. Defaults to
\code{reference$features}.}
}
\value{
A numeric matrix with the same dimensions as \code{data}, with columns in
\code{reference$features} order. Each column is divided by its \code{probs}-quantile
so the bulk of the signal maps into \eqn{[0, 1]}. Pass this matrix directly
to \code{\link[=somalign_query]{somalign_query()}}.
}
\description{
Divides each feature column by its upper quantile so that the
\code{probs}-quantile maps to 1.0. Scale-only normalisation. Returned matrix
passes directly to \code{\link[=somalign_query]{somalign_query()}}.

An optional pre-processing step that brings each marker's dynamic range into
\eqn{[0, 1]} before projection to the reference. Unlike \code{\link[=somalign_normalize]{somalign_normalize()}},
this operates entirely in raw (unscaled) space and does not use the reference
mean or standard deviation; it only uses the reference to resolve feature names.
}
\examples{
set.seed(1)
mat <- matrix(abs(rnorm(40)) * 1000, nrow = 20, ncol = 2,
              dimnames = list(NULL, c("F1", "F2")))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
normed <- somalign_quantile_normalize(mat, ref, probs = 0.999)
}
\seealso{
\code{\link[=somalign_normalize]{somalign_normalize()}}, \code{\link[=somalign_query]{somalign_query()}}
}
