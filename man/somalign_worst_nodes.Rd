% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{somalign_worst_nodes}
\alias{somalign_worst_nodes}
\title{Return worst-projecting query SOM nodes}
\usage{
somalign_worst_nodes(fit, n = 10L)
}
\arguments{
\item{fit}{A \code{somalign_fit} object.}

\item{n}{Number of nodes to return. Default \code{10}.}
}
\value{
A data frame with columns \code{query_node}, \code{query_mass},
\code{transported_mass}, \code{match_fraction}, \code{correction_allowed},
\code{correction_norm}, and \code{top_ref_label}, one row per node.
}
\description{
Returns the \code{n} query nodes with the lowest match fraction — the nodes
whose mass the OT solver could not route to the reference — sorted
ascending. Includes the dominant reference label each node maps to.
}
\examples{
set.seed(1)
mat <- matrix(rnorm(20), nrow = 10, ncol = 2,
              dimnames = list(NULL, c("F1", "F2")))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
qry <- somalign_query(mat, ref, grid = kohonen::somgrid(2, 2, "hexagonal"),
                      rlen = 5)
fit <- somalign_fit(qry, ref)
somalign_worst_nodes(fit, n = 4)
}
