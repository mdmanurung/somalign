% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anchored.R
\name{somalign_fit_anchored}
\alias{somalign_fit_anchored}
\title{Align a query SOM to a reference SOM using anchor sample pairs}
\usage{
somalign_fit_anchored(
  query,
  reference,
  anchor_old,
  anchor_new,
  rho_anchor = 1,
  epsilon = 0.1,
  rho_query = 1,
  rho_ref = 1,
  solver = c("internal", "log_domain", "auto", "annealing"),
  min_match_fraction = 0.05,
  confidence_threshold = 0.6,
  correction_min_mass = 1e-08,
  max_iter = 1000,
  tol = 1e-07,
  chunk_size = 10000L,
  correction = c("cost_bonus", "subspace", "both"),
  variance_threshold = 0.9,
  anneal_start = 10,
  anneal_stages = 10L,
  anneal_factor = NULL,
  feature_weights = NULL,
  laplacian_lambda = 0
)
}
\arguments{
\item{query}{A \code{somalign_query} object.}

\item{reference}{A \code{somalign_reference} object.}

\item{anchor_old}{Numeric matrix (n_anchors × p). Old-batch measurements
of the anchor samples. Must be \strong{raw (un-normalized) values in the same
units and preprocessing pipeline as the data used to train \code{reference}}.
Do not pre-center or pre-scale; this function applies \code{reference$center}
and \code{reference$scale} internally. Also accepts a data frame of numeric
columns.}

\item{anchor_new}{Numeric matrix (n_anchors × p). New-batch measurements
of the \strong{same} anchor samples. Must be \strong{raw (un-normalized) values in
the same units and preprocessing pipeline as \code{anchor_old}}. Do not
pre-center or pre-scale; this function applies \code{reference$center} and
\code{reference$scale} internally. Rows of \code{anchor_old} and \code{anchor_new} must
correspond to the same biological units. Also accepts a data frame of
numeric columns.}

\item{rho_anchor}{Non-negative scalar. Controls how strongly anchor pairs
bias the OT cost. At \code{rho_anchor = 0} the result equals \code{\link[=somalign_fit]{somalign_fit()}}.
Larger values reduce the effective cost for anchor-supported node pairs,
concentrating the transport plan on those routes. Typical range: 0.5--3.
Has no effect when \code{correction = "subspace"}.}

\item{epsilon}{Entropic regularisation strength (see \code{\link[=somalign_fit]{somalign_fit()}}).}

\item{rho_query}{Query-side unbalanced mass relaxation.}

\item{rho_ref}{Reference-side unbalanced mass relaxation.}

\item{solver}{Sinkhorn solver variant. See \code{\link[=somalign_fit]{somalign_fit()}}.}

\item{min_match_fraction}{Minimum transported fraction for label transfer.}

\item{confidence_threshold}{Minimum top-label probability for label transfer.}

\item{correction_min_mass}{Minimum transported mass for a node correction.}

\item{max_iter}{Maximum Sinkhorn iterations.}

\item{tol}{Sinkhorn convergence tolerance.}

\item{chunk_size}{Integer. Samples projected per chunk. Default \code{10000L}.}

\item{correction}{Character. Correction strategy — one of
\code{"cost_bonus"} (default), \code{"subspace"}, or \code{"both"}. See Details.}

\item{variance_threshold}{Numeric in (0, 1]. Cumulative singular-value-squared
fraction for selecting the rank of the batch subspace. Default \code{0.9}
(CellANOVA convention). Only used when \code{correction} is \code{"subspace"} or
\code{"both"}.}

\item{anneal_start, anneal_stages, anneal_factor}{Annealing-schedule tuning
parameters, used only when \code{solver = "annealing"}. See \code{\link[=somalign_fit]{somalign_fit()}}.}

\item{feature_weights}{Either \code{NULL} (default, squared-Euclidean cost), a
named non-negative numeric vector of explicit per-feature weights (see
\code{\link[=somalign_fit]{somalign_fit()}}), or the string \code{"anchor"} -- auto-estimates weights
from the anchor displacement matrix \code{D} via
\eqn{w_f = 1 / (\mathrm{var}(D_{\cdot f}) + \delta)}, mean-normalised.
Markers that vary most across the batch (large \code{var(D[, f])}, i.e.
batch-driven) get low weight and are cheap to transport; markers stable
across the batch get high weight and are expensive to transport,
preserving biology. The resolved vector is stored in
\code{fit$anchors$feature_weights} and
\code{fit$diagnostics$cost_metric$feature_weights}. Composes independently
with \code{correction}: the weights reshape the cost geometry, while
\code{rho_anchor}/\code{correction} bias routing -- both act on the same
underlying transport problem without conflict.}

\item{laplacian_lambda}{Non-negative scalar. Graph-Laplacian smoothing of
the node-shift field; see \code{\link[=somalign_fit]{somalign_fit()}}. When \code{correction} is
\code{"subspace"} or \code{"both"}, smoothing is applied \emph{before} the subspace
projection (smooth in full marker space, then restrict to the batch
subspace \code{V}) so the Laplacian neighbor structure is respected. Default
\code{0} (no smoothing).}
}
\value{
A \code{somalign_anchored_fit} object (also inherits \code{somalign_fit}).
}
\description{
A variant of \code{\link[=somalign_fit]{somalign_fit()}} for the case where a set of samples has been
measured in \strong{both} the old batch (reference space) and the new batch
(query space). These \emph{anchor pairs} are used to build a per-node-pair
correspondence count matrix, which is subtracted from the normalized OT
cost before the Sinkhorn solve. This makes transport along anchor-supported
routes cheaper, biasing the OT plan toward pairings that are consistent
with the observed per-sample batch displacement — while still solving a
valid optimal transport problem over the full codebook.
}
\details{
\strong{Correction modes.} Three strategies are available via the \code{correction}
argument.
\itemize{
\item \code{"cost_bonus"} (default, current behaviour): the anchor count matrix
biases the OT cost so anchor-supported node pairs are cheaper; the
resulting node shifts are applied to the full feature space.
\item \code{"subspace"}: a batch subspace \eqn{V_{\text{batch}}} is estimated by
SVD of the anchor displacement matrix
\eqn{D = X_{\text{old}} - X_{\text{new}}} (n_anchors × p). Because each
row of \eqn{D} is a \emph{same-biological-unit} before–after measurement, the
dominant singular vectors isolate the true batch direction. Node shifts
from a \emph{plain} OT solve (no cost bonus) are then projected onto
\eqn{V_{\text{batch}}}: only the batch-direction component is applied.
Biological variation orthogonal to \eqn{V_{\text{batch}}} is preserved.
A synthetic validation shows the orthogonal component survives at
~99.7\% (1.496 vs ideal 1.500) while \code{"cost_bonus"} erases it.
The rank \eqn{r} is the smallest index where the cumulative squared
singular values reach \code{variance_threshold} (default 0.9).
\eqn{D} is \strong{not centred} — the mean batch direction is the dominant
structure we want to capture.
\item \code{"both"}: applies the cost bonus to the OT solve \emph{and} restricts the
resulting shifts to \eqn{V_{\text{batch}}}.
}

\code{"subspace"} and \code{"both"} expose \code{fit$anchors$batch_subspace} (a list with
\code{V}, \code{rank}, \code{variance_explained}). \code{"cost_bonus"} sets this to \code{NULL}.

\strong{Topology preservation and epsilon.} Empirically, the primary driver of
topology/structure damage from batch correction is \code{epsilon}, not
\code{rho_anchor}. Higher epsilon blurs the transport plan across a wider
neighbourhood, causing the corrected codebook to collapse biologically
distinct populations (H0 component merging). Subspace-restricted modes
(\code{"subspace"} or \code{"both"}) substantially reduce merging at any given
epsilon because shifts are confined to the batch-variation subspace, leaving
orthogonal biological variation intact. As a result, choosing epsilon
involves a genuine trade-off: higher epsilon is more numerically stable for
the Sinkhorn solver, but lower epsilon preserves more topology. Before
committing to an epsilon, run
\code{somalign_epsilon_sweep(..., topology = TRUE)} alongside
\code{\link[=somalign_select_epsilon]{somalign_select_epsilon()}} and inspect both the phase-transition criterion
and the \code{biggest_merge_mass_frac} column -- the two criteria can disagree,
especially at small epsilon near numerical instability.

\strong{Cost modification.} Let \eqn{C} be the M×K codebook distance matrix
normalised by its median positive entry (as in \code{\link[=somalign_fit]{somalign_fit()}}). Each
anchor pair is projected onto both codebooks to build a count matrix
\eqn{A} where \eqn{A_{kl}} is the number of anchor pairs mapping to query
node \eqn{k} and reference node \eqn{l}. The query SOM was trained on
new-batch data, so the \emph{new-batch} anchor measurement is projected onto the
query codebook to identify query node \eqn{k}; the reference SOM was trained
on old-batch data, so the \emph{old-batch} anchor measurement is projected onto
the reference codebook to identify reference node \eqn{l}.
The modified cost is
\deqn{\tilde{C}_{kl} = \max\!\bigl(C_{kl} - \rho_{\mathrm{anchor}} \cdot
  A_{kl} / n_{\mathrm{anchors}},\; 0\bigr).}
Pairs with many anchor observations get cost reduced toward zero (free
transport), while uncovered pairs retain their original cost. Non-negativity
is enforced by the \eqn{\max(\cdot, 0)} clamp.

\strong{Clamp behaviour at large \code{rho_anchor}.} When the anchor bonus exceeds
\eqn{C_{kl}}, the effective cost is clamped to zero. All such pairs then
have identical effective cost and the transport mass among them is determined
by entropic regularisation alone rather than by relative anchor counts.
The clamp is required to keep costs non-negative; at very large \code{rho_anchor}
the plan for anchor-covered pairs becomes more entropic, not more
concentrated. A practical upper bound is \code{rho_anchor * max(A) / n_anchors <= 1}, i.e., even the most-supported pair reduces cost by at most one
median squared-distance unit.

\strong{Fallback for uncovered nodes.} Query nodes with no anchor samples retain
their original pairwise costs, so the transport plan for those nodes is
determined entirely by the OT objective — the same as \code{\link[=somalign_fit]{somalign_fit()}}.
Inspect \verb{$anchors$coverage_fraction} to see what fraction of query nodes
had at least one anchor pair.

\strong{Return value.} The object has class \code{c("somalign_anchored_fit", "somalign_fit")}, so all downstream functions that accept a
\code{somalign_fit} object (\code{\link[=somalign_results]{somalign_results()}}, \code{\link[=somalign_diagnostics]{somalign_diagnostics()}})
work unchanged. An additional \verb{$anchors} list element is attached:
\describe{
\item{\code{n_anchors}}{Number of anchor pairs supplied.}
\item{\code{rho_anchor}}{The value of \code{rho_anchor} used.}
\item{\code{correction}}{The correction mode: \code{"cost_bonus"}, \code{"subspace"}, or \code{"both"}.}
\item{\code{nodes_covered}}{Number of query nodes with ≥ 1 anchor pair.}
\item{\code{coverage_fraction}}{\code{nodes_covered / nrow(query$codebook)}.}
\item{\code{batch_subspace}}{For \code{"subspace"} and \code{"both"} modes: a list with
\code{V} (p × rank matrix), \code{rank} (integer), and \code{variance_explained}
(cumulative variance at the selected rank). \code{NULL} for \code{"cost_bonus"}.}
\item{\code{displacements}}{The scaled anchor displacement matrix
\eqn{D = X_{\text{old,scaled}} - X_{\text{new,scaled}}}
(n_anchors × p), always stored regardless of \code{correction} mode. Used by
\code{\link[=somalign_subspace_sensitivity]{somalign_subspace_sensitivity()}} and \code{\link[=somalign_exclusion_test]{somalign_exclusion_test()}}.}
}
}
\note{
At small \code{epsilon} with high anchor coverage the anchor bonus zeros out
many entries of the normalised cost matrix, which sharpens the Sinkhorn
kernel and can drive the remaining entries toward numerical underflow. If
the solver warns about kernel underflow, pass \code{solver = "log_domain"} or
\code{solver = "annealing"}, both of which work in log-potential space and
avoid the issue.
}
\examples{
set.seed(1)
p   <- 3L
mat <- rbind(
  matrix(rnorm(20 * p, mean = -2), ncol = p),
  matrix(rnorm(20 * p, mean =  2), ncol = p)
)
colnames(mat) <- paste0("F", seq_len(p))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
shifted <- mat + 0.5
qry <- somalign_query(shifted, ref, grid = kohonen::somgrid(2, 2, "hexagonal"),
                      rlen = 5)
# Use 10 samples as anchors measured in both batches
anc_idx <- 1:10
fit <- somalign_fit_anchored(qry, ref,
                              anchor_old = mat[anc_idx, , drop = FALSE],
                              anchor_new = shifted[anc_idx, , drop = FALSE],
                              rho_anchor = 1)
fit$anchors
}
\seealso{
\code{\link[=somalign_fit]{somalign_fit()}} for the unanchored variant.
}
