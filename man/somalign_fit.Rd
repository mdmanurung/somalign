% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{somalign_fit}
\alias{somalign_fit}
\title{Align a query SOM to a reference SOM}
\usage{
somalign_fit(
  query,
  reference,
  epsilon = 0.1,
  rho_query = 1,
  rho_ref = 1,
  solver = c("internal", "log_domain", "auto", "annealing"),
  min_match_fraction = 0.05,
  confidence_threshold = 0.6,
  correction_min_mass = 1e-08,
  max_iter = 1000,
  tol = 1e-07,
  chunk_size = 10000L,
  diagonal_boost = 0,
  label_guided = FALSE,
  anneal_start = 10,
  anneal_stages = 10L,
  anneal_factor = NULL,
  feature_weights = NULL
)
}
\arguments{
\item{query}{A \code{somalign_query} object.}

\item{reference}{A \code{somalign_reference} object.}

\item{epsilon}{Entropic regularisation strength. The cost matrix is
normalised by its median positive entry before computing the Sinkhorn
kernel, so \code{epsilon} is approximately scale- and dimension-invariant.
The default \code{0.1} gives a sharp transport plan that preserves cell-type
specificity for typical z-scored SOM codebooks. Larger values (0.3–0.5)
produce smoother, more diffuse plans that can help convergence on noisy
or high-dimensional data but dilute label posteriors and increase
barycentric shrinkage in the corrected projection. Very small values
(< 0.05) make the transport increasingly discrete and may require
\code{solver = "log_domain"} for numerical stability. The normalisation scale
is stored in \code{diagnostics$solver$cost_scale}.}

\item{rho_query}{Query-side unbalanced mass relaxation.}

\item{rho_ref}{Reference-side unbalanced mass relaxation.}

\item{solver}{Sinkhorn solver variant. \code{"internal"} (default) and \code{"auto"}
both use the primal-domain scaling iteration. \code{"log_domain"} uses a
numerically stable log-potential variant that avoids kernel underflow for
small \code{epsilon} or high-dimensional codebooks; it is slower per iteration
but tolerates cost/epsilon ratios that cause \code{"internal"} to warn.
\code{"annealing"} runs the log-domain solver across a geometric epsilon
cooling schedule (starting at \code{anneal_start * epsilon}, cooling to
\code{epsilon} over \code{anneal_stages} stages), warm-starting each stage from the
previous stage's dual potentials. Recommended for \code{label_guided} fits or
any fit with small \code{epsilon} (< 0.05) where cold-start Sinkhorn is slow
or non-convergent; never underflows, since it never exponentiates the
kernel.}

\item{min_match_fraction}{Minimum transported fraction required before a
query node label transfer is accepted.}

\item{confidence_threshold}{Minimum top-label probability required before a
query node label transfer is accepted.}

\item{correction_min_mass}{Minimum transported node mass required before a
correction shift is applied. Corrections also require the node match
fraction to pass \code{min_match_fraction}.}

\item{max_iter}{Maximum internal Sinkhorn iterations.}

\item{tol}{Internal Sinkhorn convergence tolerance.}

\item{chunk_size}{Integer. Number of samples to project per chunk when
computing nearest reference node. Use \code{Inf} or \code{NULL} for no chunking
(allocates a full n_samples x n_nodes matrix). Default \code{10000L}.}

\item{diagonal_boost}{Non-negative scalar. Amount by which to reduce the
normalised OT cost for each query node's nearest reference node. A
positive value makes the transport plan prefer identity-like mappings,
shrinking over-correction when the two codebooks are already close. Zero
(default) leaves the cost unchanged. Values around 0.1--0.5 are a
reasonable starting point; very large values concentrate all mass on the
diagonal and the plan degrades toward simple nearest-neighbour assignment.}

\item{label_guided}{Logical. When \code{TRUE}, uses \code{query$label_prob} and
\code{reference$label_prob} to add a large cost penalty for node pairs whose
dominant labels disagree, constraining OT to transport mass predominantly
between concordant cell-type nodes. Nodes where the maximum label
probability is below 0.5 are treated as unlabeled and are never penalized.
Errors if \code{label_guided = TRUE} but either \code{label_prob} is \code{NULL}.}

\item{anneal_start}{Positive scalar >= 1. When \code{solver = "annealing"}, the
starting epsilon is \code{anneal_start * epsilon}. Default \code{10}. Ignored when
\code{solver != "annealing"}.}

\item{anneal_stages}{Positive integer. Number of cooling stages in the
annealing schedule, including the final stage at the target \code{epsilon}.
Default \code{10L}. A value of \code{1} degenerates to a cold-start log-domain
solve. Ignored when \code{solver != "annealing"}.}

\item{anneal_factor}{Positive scalar < 1, or \code{NULL} (default). When not
\code{NULL}, overrides the auto-computed per-stage cooling ratio. Ignored when
\code{solver != "annealing"}.}

\item{feature_weights}{Either \code{NULL} (default, squared-Euclidean cost) or a
named non-negative numeric vector with one entry per feature (explicit
diagonal Mahalanobis weights on the OT cost). Weights are applied as
\code{sqrt(w_f)} per-column scaling of both codebooks before the squared
Euclidean distance is computed, yielding cost
\eqn{\sum_f w_f (q_{if} - r_{jf})^2}. The resolved vector is stored in
\code{fit$diagnostics$cost_metric$feature_weights}. Projection and threshold
distances (\code{somalign_results()}) are unaffected -- weighting applies only
to the OT cost. See \code{\link[=somalign_fit_anchored]{somalign_fit_anchored()}} for \code{"anchor"}, which
auto-estimates weights from anchor displacements.}
}
\value{
A \code{somalign_fit} object.
}
\description{
Align a query SOM to a reference SOM
}
\details{
The transport plan row sums will not equal \code{query$node_masses} exactly -- this
is by design. Unbalanced optimal transport allows mass destruction, so some
query mass may be absorbed rather than transported. Deviation grows with lower
\code{rho_query} / \code{rho_ref} values and higher \code{epsilon}. Use
\code{diagnostics$ot$max_row_mass_error} to quantify the deviation in a given fit;
for near-balanced data, increase \code{rho_query} (e.g. \code{rho_query = 10}) to
enforce tighter marginal constraints. A warning is emitted automatically when
more than 50\% of query mass is destroyed.

The cost matrix is normalised by its median positive entry before the
Sinkhorn kernel is computed. This makes \code{epsilon} scale- and
dimension-invariant: the same value produces the same degree of regularisation
regardless of the number of features or the spread of codebook coordinates.
The raw normalisation factor is stored as \code{diagnostics$solver$cost_scale}.
}
\examples{
set.seed(1)
mat <- matrix(rnorm(20), nrow = 10, ncol = 2,
              dimnames = list(NULL, c("F1", "F2")))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
qry <- somalign_query(mat, ref, grid = kohonen::somgrid(2, 2, "hexagonal"),
                      rlen = 5)
fit <- somalign_fit(qry, ref)
}
