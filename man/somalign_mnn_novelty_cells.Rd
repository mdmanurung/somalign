% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mnn-novelty.R
\name{somalign_mnn_novelty_cells}
\alias{somalign_mnn_novelty_cells}
\title{Broadcast the MNN novelty flag from query nodes to individual cells}
\usage{
somalign_mnn_novelty_cells(fit, mnn_unmatched)
}
\arguments{
\item{fit}{A \code{somalign_fit} object.}

\item{mnn_unmatched}{A named logical vector of length \code{n_query_nodes},
as returned by \code{\link[=somalign_mnn_novelty]{somalign_mnn_novelty()}}.}
}
\value{
A logical vector of length equal to the number of query cells.
\code{TRUE} for cells assigned to an MNN-unmatched query node.
}
\description{
Maps the per-query-node \code{mnn_unmatched} vector returned by
\code{\link[=somalign_mnn_novelty]{somalign_mnn_novelty()}} to individual cells using each cell's query-SOM
node assignment (\code{fit$query$sample_unit}).
}
\examples{
set.seed(42)
mat <- matrix(rnorm(100), nrow = 50, ncol = 2,
              dimnames = list(NULL, c("F1", "F2")))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
qry <- somalign_query(mat, ref, grid = kohonen::somgrid(2, 2, "hexagonal"),
                      rlen = 5)
fit <- somalign_fit(qry, ref)
flags <- somalign_mnn_novelty(fit)
cell_flags <- somalign_mnn_novelty_cells(fit, flags)
}
