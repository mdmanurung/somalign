% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{somalign_query}
\alias{somalign_query}
\title{Prepare query data and attach or train a query SOM}
\usage{
somalign_query(
  data,
  reference,
  som_query = NULL,
  grid = NULL,
  rlen = 100,
  alpha = c(0.05, 0.01),
  features = NULL,
  ...
)
}
\arguments{
\item{data}{Numeric query data.}

\item{reference}{A \code{somalign_reference} object.}

\item{som_query}{Optional query SOM or SOM-like object with a codebook. The
codebook must be in the reference-scaled feature space, i.e. trained on
query data transformed with \code{reference$center} and \code{reference$scale}.}

\item{grid}{Optional \code{kohonen::somgrid()} object when \code{som_query} is omitted.}

\item{rlen}{Number of SOM training iterations passed to \code{\link[kohonen:som]{kohonen::som()}}.}

\item{alpha}{Learning-rate schedule passed to \code{\link[kohonen:som]{kohonen::som()}}.}

\item{features}{Optional feature names. Defaults to the reference feature
order.}

\item{...}{Additional arguments passed to \code{\link[kohonen:som]{kohonen::som()}}.}
}
\value{
A \code{somalign_query} object.
}
\description{
Query data are always scaled with the saved reference center and scale.
}
\examples{
set.seed(1)
mat <- matrix(rnorm(20), nrow = 10, ncol = 2,
              dimnames = list(NULL, c("F1", "F2")))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
qry <- somalign_query(mat, ref, grid = kohonen::somgrid(2, 2, "hexagonal"),
                      rlen = 5)
}
