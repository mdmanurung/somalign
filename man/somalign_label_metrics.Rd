% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{somalign_label_metrics}
\alias{somalign_label_metrics}
\title{Label-transfer accuracy metrics}
\usage{
somalign_label_metrics(predicted, truth, accepted = NULL)
}
\arguments{
\item{predicted}{Character vector of predicted labels (\code{NA} = abstain).}

\item{truth}{Character vector of ground-truth labels, same length.}

\item{accepted}{Optional logical vector, same length. When supplied, only
\code{accepted} (and non-\code{NA}) predictions are scored; the rest are abstentions.
When \code{NULL} (default), all non-\code{NA} predictions are scored.}
}
\value{
A list of class \code{somalign_label_metrics} with \code{accuracy},
\code{macro_f1}, \code{mcc}, \code{per_class} (data frame), \code{confusion} (table), \code{n}
(scored predictions), \code{coverage}, and \code{accuracy_all}.
}
\description{
Computes overall accuracy, macro-averaged F1, multiclass Matthews
correlation coefficient (Gorodkin's \eqn{R_K}), per-class
precision/recall/F1, the confusion matrix, and coverage (the fraction of
cells with an accepted prediction), given predicted and ground-truth labels.
}
\details{
Metrics are computed on the \emph{accepted} predictions only; \code{coverage} reports
what fraction of cells that is. \code{accuracy_all} additionally scores
abstentions (rejected or \code{NA} predictions) as wrong, for a
coverage-penalised view.
}
\examples{
truth <- rep(c("A", "B", "C"), each = 10)
pred  <- truth; pred[c(1, 12, 25)] <- "B"
somalign_label_metrics(pred, truth)
}
