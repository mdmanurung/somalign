% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grow.R
\name{somalign_grow_reference}
\alias{somalign_grow_reference}
\title{Grow a reference codebook with novel-population nodes (Growing Neural Gas)}
\usage{
somalign_grow_reference(
  reference,
  new_data,
  max_new_nodes = 20L,
  lambda = 100L,
  epsilon_new = 0.05,
  age_max = 50L,
  error_decay = 0.995,
  n_epochs = 5L,
  seed = NULL,
  novel_label = "novel"
)
}
\arguments{
\item{reference}{A \code{somalign_reference} object produced by
\code{\link[=somalign_train_reference]{somalign_train_reference()}} or \code{\link[=somalign_reference_from_nodes]{somalign_reference_from_nodes()}}.}

\item{new_data}{A numeric matrix of cells (rows) × features (columns).  Must
carry the same feature names as \code{reference$features}.}

\item{max_new_nodes}{Maximum number of new nodes to insert (default \code{20}).
Growth stops when this cap is reached even if the error criterion would
still trigger insertions.}

\item{lambda}{Insert a new node every \code{lambda} input presentations
(default \code{100}).}

\item{epsilon_new}{Learning rate applied to newly inserted (unfrozen) nodes
when they win or are topological neighbours of a winner (default \code{0.05}).}

\item{age_max}{Maximum edge age before an edge is pruned (default \code{50}).}

\item{error_decay}{Multiplicative decay applied to the accumulated error at
every input presentation (default \code{0.995}).}

\item{n_epochs}{Number of full passes over \code{new_data} during GNG training
(default \code{5}).  Multiple epochs improve convergence; inputs are shuffled
each epoch.}

\item{seed}{Integer random seed used for epoch-wise shuffling (default
\code{NULL}, no seeding).}

\item{novel_label}{Character scalar.  The label assigned to grown nodes in
the extended \code{label_prob} matrix (default \code{"novel"}).}
}
\value{
A \code{somalign_reference} built with \code{\link[=somalign_reference_from_nodes]{somalign_reference_from_nodes()}}:
\describe{
\item{Codebook}{Original rows unchanged; grown nodes appended.}
\item{distance_quantiles}{\code{Inf} for grown rows — never flagged as outside
the reference.}
\item{global_distance_quantiles}{Inherited from \code{reference} (column-wise
max over the original nodes only).}
\item{label_prob}{Original labels preserved; a \code{novel_label} column
appended.  Original nodes get 0 in \code{novel_label}; grown nodes get 1.}
\item{node_masses}{Tabulated from \code{new_data} assignments over the
extended codebook and re-normalised to sum 1.}
\item{som_ref}{\code{NULL} (node-level reference without topology).}
}
}
\description{
Seeds a Growing Neural Gas (GNG) from an existing trained reference codebook
and inserts new nodes for novel populations encountered in \code{new_data} WITHOUT
retraining or moving the original codebook vectors.  The original nodes are
completely frozen: their positions are never updated, regardless of whether
they win or are topological neighbours of a winner.
}
\details{
\strong{Algorithm (GNG with frozen originals)}
\enumerate{
\item Initialise codebook \code{W} from \code{reference$codebook} (N_orig nodes).  All
N_orig indices are permanently \emph{frozen}.
\item Initialise an empty edge graph and per-node cumulative error vector.
\item For each input \code{x} (shuffled per epoch, repeated for \code{n_epochs}):
a. Find s1 (nearest node) and s2 (second-nearest node) in \code{W}.
b. Increment \code{error[s1]} by \verb{||x - W[s1,]||^2}.
c. Age all edges incident to s1 by 1; remove edges older than \code{age_max}
and prune isolated nodes that are \strong{not} frozen.
d. \strong{Move} s1 and its topological neighbours by \code{epsilon_new * (x - W[i,])}
\strong{only if} the node is NOT frozen.
e. Create / refresh (age = 0) the edge s1 -- s2.
f. Every \code{lambda} presentations: insert a new node between the
highest-error node \code{q} and its highest-error neighbour \code{f}; split error;
stop if \code{max_new_nodes} reached.
g. Apply \code{error_decay} to all errors.
\item Assign each \code{new_data} row to the nearest node in the final extended \code{W}.
\item Build and return an extended reference via \code{\link[=somalign_reference_from_nodes]{somalign_reference_from_nodes()}}.
}

\strong{Prototype note.} Node masses are computed from \code{new_data} alone.  The
function does not re-project original reference cells; mass on original nodes
reflects coverage of \code{new_data}, not the training corpus.  Use downstream
\code{\link[=somalign_fit]{somalign_fit()}} with \code{laplacian_lambda = 0} (required because \code{som_ref} is
\code{NULL} in the returned object).
}
\seealso{
\code{\link[=somalign_reference_from_nodes]{somalign_reference_from_nodes()}}, \code{\link[=somalign_fit]{somalign_fit()}}
}
