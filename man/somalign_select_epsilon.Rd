% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{somalign_select_epsilon}
\alias{somalign_select_epsilon}
\title{Select epsilon from an epsilon-sweep curve}
\usage{
somalign_select_epsilon(
  query,
  reference,
  epsilon = c(0.02, 0.05, 0.1, 0.2, 0.5, 1),
  rho_query = 1,
  rho_ref = 1,
  method = c("critical", "elbow", "entropy_fraction"),
  entropy_fraction = 0.9,
  solver = c("log_domain", "internal", "auto"),
  max_iter = 1000,
  tol = 1e-07,
  diagonal_boost = 0,
  label_guided = FALSE,
  parallel = FALSE
)
}
\arguments{
\item{query}{A \code{somalign_query} object.}

\item{reference}{A \code{somalign_reference} object.}

\item{epsilon}{Numeric vector of candidate epsilon values, or \code{NULL} to use
the default grid. Default \code{c(0.02, 0.05, 0.1, 0.2, 0.5, 1.0)}.}

\item{rho_query, rho_ref}{Marginal relaxations passed to the OT solver.}

\item{method}{Character. \code{"critical"} (default) uses the susceptibility
peak's recommended epsilon (\code{0.3 * epsilon_c}). \code{"elbow"} uses the
max-second-difference rule on the mutual-information-vs-cost curve.
\code{"entropy_fraction"} chooses the largest (most-regularized) epsilon that
still retains \code{entropy_fraction * max(mutual_information)}.}

\item{entropy_fraction}{Numeric in (0, 1]. Target fraction of maximum
mutual information when \code{method = "entropy_fraction"}. Default \code{0.90}.}

\item{solver, max_iter, tol}{Sinkhorn solver parameters.}

\item{diagonal_boost, label_guided, parallel}{See \code{\link[=somalign_epsilon_sweep]{somalign_epsilon_sweep()}}.}
}
\value{
A list of class \code{"somalign_epsilon_selection"} with
\code{selected_epsilon}, \code{curve} (the sweep's \code{table}), and \code{method}.
}
\description{
Sweeps a grid of epsilon values via \code{\link[=somalign_epsilon_sweep]{somalign_epsilon_sweep()}} and selects
a recommended value using one of three criteria: the susceptibility
(critical-epsilon) peak, the mutual-information-vs-cost elbow, or the
smallest epsilon retaining a target fraction of maximum mutual information.
}
\examples{
set.seed(1)
mat <- matrix(rnorm(20), nrow = 10, ncol = 2,
              dimnames = list(NULL, c("F1", "F2")))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
qry <- somalign_query(mat, ref, grid = kohonen::somgrid(2, 2, "hexagonal"),
                      rlen = 5)
somalign_select_epsilon(qry, ref, epsilon = c(0.05, 0.1, 0.2))
}
\seealso{
\code{\link[=somalign_epsilon_sweep]{somalign_epsilon_sweep()}}
}
