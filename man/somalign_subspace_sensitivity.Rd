% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{somalign_subspace_sensitivity}
\alias{somalign_subspace_sensitivity}
\title{Subspace sensitivity analysis for anchored batch correction}
\usage{
somalign_subspace_sensitivity(
  fit,
  n_boot = 200L,
  variance_threshold = NULL,
  conf_level = 0.95,
  seed = 1L
)
}
\arguments{
\item{fit}{A \code{somalign_anchored_fit} with \code{correction = "subspace"} or
\code{"both"}.}

\item{n_boot}{Positive integer. Number of bootstrap replicates of \code{D}.
Default \code{200L}.}

\item{variance_threshold}{Numeric in (0, 1], or \code{NULL}. Variance threshold
for SVD rank selection in bootstrap replicates. \code{NULL} (default) reuses
the threshold from the original fit (\code{fit$anchors$variance_threshold}).}

\item{conf_level}{Numeric in (0, 1). Confidence level for the node-shift
confidence intervals. Default \code{0.95}.}

\item{seed}{Integer or \code{NULL}. RNG seed for reproducibility (restored on
exit; does not leak into the caller's session). Default \code{1L}; \code{NULL}
disables seeding.}
}
\value{
A list of class \code{somalign_subspace_sensitivity}:
\describe{
\item{\code{node_correction_ci}}{M x 2 matrix (lower, upper) -- bootstrap CI
on each node's corrected-shift norm.}
\item{\code{node_shift_ci}}{M x p x 2 array -- per-feature bootstrap CI.}
\item{\code{subspace_angles}}{n_boot x rank matrix of principal angles
(degrees) between the fitted \code{V} and each bootstrap \code{V_b}.}
\item{\code{tipping_angle_deg}}{Length-M vector. For rank-1 \code{V}, the analytic
angle \eqn{\arcsin(|\hat{s}_i \cdot v|)} between a node's unit raw
shift and \code{v}; for rank > 1, a conservative proxy from the minimum
singular value of the (1 x rank) projection. \code{NA} for disallowed or
zero-norm nodes. Small (< 10 degrees) signals a fragile correction;
large (> 45 degrees) indicates robustness.}
\item{\code{anchor_leverage}}{Length-n_anchors vector: the maximum principal
angle (degrees) between \code{V} and the leave-one-out subspace when that
anchor is dropped -- a Cook's-distance analog for anchor influence.}
\item{\code{n_boot}, \code{conf_level}, \code{subspace_rank}, \code{n_anchors},
\code{variance_threshold}}{Metadata.}
}
}
\description{
Bootstraps the anchor displacement matrix \code{D} to quantify how stable the
estimated batch subspace \code{V} is, and propagates that uncertainty to
per-node correction confidence intervals and "tipping angles" -- the
smallest rotation of \code{V} that would erase a node's correction.
}
\details{
Raw pre-projection node shifts are recovered from stored fit components
(\code{fit$correspondence}, both codebooks), not from \code{fit$node_shifts}, which
stores \emph{post-projection} shifts (\verb{S \\\%*\\\% V \\\%*\\\% t(V)}) for subspace fits --
using it directly as the raw shift would double-project and produce
invalid tipping angles.

Bootstrap replicate rank is fixed to the point-estimate rank of the
original fit (not re-selected per replicate), so principal angles compare
subspaces of the same dimension; if a replicate's own rank is lower,
its \code{V_b} is zero-padded.
}
\section{Role}{
 A \strong{diagnostic on the correction path} (how trustworthy the
subspace correction is), not a label-transfer diagnostic. Label transfer
does not use the subspace or \code{node_shifts}.
}

\examples{
set.seed(1)
p <- 3L
mat <- rbind(
  matrix(rnorm(20 * p, mean = -2), ncol = p),
  matrix(rnorm(20 * p, mean =  2), ncol = p)
)
colnames(mat) <- paste0("F", seq_len(p))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
shifted <- mat + 0.5
qry <- somalign_query(shifted, ref, grid = kohonen::somgrid(2, 2, "hexagonal"),
                      rlen = 5)
anc_idx <- 1:10
fit <- somalign_fit_anchored(qry, ref,
                              anchor_old = mat[anc_idx, , drop = FALSE],
                              anchor_new = shifted[anc_idx, , drop = FALSE],
                              rho_anchor = 1, correction = "subspace")
somalign_subspace_sensitivity(fit, n_boot = 50L)
}
\seealso{
\code{\link[=somalign_fit_anchored]{somalign_fit_anchored()}}, \code{\link[=somalign_exclusion_test]{somalign_exclusion_test()}}
}
