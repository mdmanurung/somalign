% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{somalign_calibration}
\alias{somalign_calibration}
\title{Confidence calibration of label transfer}
\usage{
somalign_calibration(score, correct, n_bins = 10L)
}
\arguments{
\item{score}{Numeric vector in [0, 1]: the confidence/margin per prediction.}

\item{correct}{Logical vector, same length: whether each prediction was right.}

\item{n_bins}{Positive integer. Number of equal-width bins over [0, 1].
Default \code{10L}.}
}
\value{
A list of class \code{somalign_calibration} with \code{table} (per-bin
\code{score_mean}, \code{accuracy}, \code{n}), \code{ece}, \code{mce}, \code{brier}, and \code{n}.
}
\description{
Bins predictions by a confidence-like score in [0, 1] (e.g. the transfer
confidence or margin) and compares mean score to empirical accuracy per bin,
yielding a reliability table plus the expected (ECE) and maximum (MCE)
calibration error and the top-label Brier score. A well-calibrated model has
mean score ~ accuracy in every bin (ECE ~ 0).
}
\examples{
set.seed(1)
score <- runif(200)
correct <- runif(200) < score        # perfectly calibrated by construction
somalign_calibration(score, correct)
}
