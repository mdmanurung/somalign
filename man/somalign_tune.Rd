% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{somalign_tune}
\alias{somalign_tune}
\title{Tune transport-plan knobs against label-transfer accuracy}
\usage{
somalign_tune(
  data,
  labels,
  grid,
  param_grid,
  k = 5L,
  metric = c("mcc", "macro_f1", "accuracy", "ece"),
  stratify = TRUE,
  rlen = 20L,
  min_match_fraction = 0.05,
  confidence_threshold = 0.6,
  solver = "internal",
  max_iter = 1000,
  tol = 1e-07,
  n_bins = 10L,
  seed = 1L
)
}
\arguments{
\item{data}{Numeric cell-by-feature matrix.}

\item{labels}{Character vector of per-cell labels.}

\item{grid}{A \code{kohonen::somgrid}.}

\item{param_grid}{A data frame (one row per combination of the scalar knobs
\code{epsilon}, \code{rho_query}, \code{rho_ref}, \code{diagonal_boost}) or a list of named
lists (which may additionally carry \code{feature_weights}). Each must specify
\code{epsilon}.}

\item{k}{Folds. Default \code{5L}.}

\item{metric}{Objective to optimise: \code{"mcc"} (default), \code{"macro_f1"},
\code{"accuracy"} (all maximised) or \code{"ece"} (minimised).}

\item{stratify, rlen, seed}{As in \code{\link[=somalign_cross_validate]{somalign_cross_validate()}}.}

\item{min_match_fraction, confidence_threshold}{Label-acceptance gates,
matching \code{\link[=somalign_fit]{somalign_fit()}} defaults.}

\item{solver, max_iter, tol}{OT solver settings.}

\item{n_bins}{Calibration bins for the \code{ece} column. Default \code{10L}.}
}
\value{
A list of class \code{somalign_tune} with \code{best} (the winning combo as a
one-row data frame), \code{best_params} (named list), \code{grid} (all combinations
with their CV metrics), and \code{metric}.
}
\description{
The supervised counterpart to \code{\link[=somalign_select_epsilon]{somalign_select_epsilon()}}: instead of an
unsupervised plan-geometry criterion, it selects transport-plan parameters
by cross-validated label-transfer performance. For each parameter
combination it runs stratified k-fold CV (reusing pre-trained SOMs per fold
for efficiency -- plan knobs change only the OT solve, not the SOMs) and
scores pooled held-out predictions with \code{\link[=somalign_label_metrics]{somalign_label_metrics()}}.
}
\details{
Tunable knobs are those that shape the transport plan without needing
anchors or query labels: \code{epsilon}, \code{rho_query}, \code{rho_ref},
\code{diagonal_boost}, and \code{feature_weights} (a numeric per-feature vector).
\code{label_guided} and \code{rho_anchor} are out of scope here (they require a
labelled query SOM or anchor pairs, respectively).
}
\examples{
\donttest{
if (requireNamespace("kohonen", quietly = TRUE)) {
  set.seed(1)
  x <- rbind(matrix(rnorm(200, -2), ncol = 2), matrix(rnorm(200, 2), ncol = 2))
  colnames(x) <- c("f1", "f2")
  lab <- rep(c("low", "high"), each = 100)
  tuned <- somalign_tune(x, lab, grid = kohonen::somgrid(2, 2, "hexagonal"),
    param_grid = data.frame(epsilon = c(0.05, 0.1, 0.2)), k = 3)
  tuned$best_params$epsilon
}
}
}
