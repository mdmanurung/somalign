% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anchored.R
\name{somalign_exclusion_test}
\alias{somalign_exclusion_test}
\title{Anchor exclusion-restriction test}
\usage{
somalign_exclusion_test(fit, n_perm = 999L, seed = 1L)
}
\arguments{
\item{fit}{A \code{somalign_anchored_fit} object from
\code{somalign_fit_anchored(..., correction = "subspace")} or \code{"both"}.}

\item{n_perm}{Positive integer. Number of permutation replicates. Default \code{999L}.}

\item{seed}{Integer or \code{NULL}. RNG seed for reproducibility (restored on
exit; does not leak into the caller's session). Default \code{1L}; \code{NULL}
disables seeding.}
}
\value{
A list of class \code{somalign_exclusion_test} with \code{sv_observed},
\code{sv_null}, \code{p_value}, \code{null_quantiles}, \code{relative_stat}, \code{rank_used},
\code{n_anchors}, \code{n_features}, \code{verdict} (\code{"pass"} if \code{p > 0.1}, \code{"warn"} if
in \verb{[0.05, 0.1]}, \code{"fail"} if \verb{< 0.05}), and \code{feature_residual_norm}
(per-feature \verb{||R[, j]||}, identifying which markers drive a violation).
}
\description{
Permutation test of whether the anchor displacement matrix carries
\emph{coherent, structured} signal orthogonal to the estimated batch subspace --
an overidentification-style check of the instrumental-variable assumption
underlying \code{\link[=somalign_fit_anchored]{somalign_fit_anchored()}}'s \code{correction = "subspace"} mode:
anchors should isolate the batch direction, not biology.
}
\details{
The statistic is the leading singular value of the orthogonal residual
\eqn{R = D (I - V V^\top)}, where \eqn{D} is the scaled anchor displacement
matrix (\code{fit$anchors$displacements}) and \eqn{V} is the estimated batch
subspace (\code{fit$anchors$batch_subspace$V}). The null distribution is
generated by permuting each feature (column) of \eqn{R} independently
across anchors, which preserves each feature's own variance but destroys
any coherent cross-feature direction. A small p-value means \eqn{R}'s
features move together more than chance recombination would produce --
i.e., the anchors carry a real, structured direction that \code{correction = "subspace"} is \emph{not} removing (a batch-direction violation, or biology
leaking through the anchors). A large p-value supports the assumption that
anchors capture only batch structure.

Row permutation of \eqn{R} or \eqn{D} would \emph{not} work here: row
permutation is an orthogonal transformation and leaves every singular
value of a matrix exactly invariant, so it cannot detect anything.
Column-wise permutation is required because it is not an orthogonal row
transform and genuinely decorrelates features.

The test has no power when \code{variance_threshold = 1} (V spans the full
feature space, so R is identically zero), and low power when
\code{n_anchors < 3 * rank}, or when the true structure is fully absorbed into
\eqn{V} (inspect \code{fit$anchors$batch_subspace$rank} and
\code{variance_explained}).
}
\examples{
set.seed(1)
p <- 3L
mat <- rbind(
  matrix(rnorm(20 * p, mean = -2), ncol = p),
  matrix(rnorm(20 * p, mean =  2), ncol = p)
)
colnames(mat) <- paste0("F", seq_len(p))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
shifted <- mat + 0.5
qry <- somalign_query(shifted, ref, grid = kohonen::somgrid(2, 2, "hexagonal"),
                      rlen = 5)
anc_idx <- 1:10
fit <- somalign_fit_anchored(qry, ref,
                              anchor_old = mat[anc_idx, , drop = FALSE],
                              anchor_new = shifted[anc_idx, , drop = FALSE],
                              rho_anchor = 1, correction = "subspace")
somalign_exclusion_test(fit, n_perm = 199L)
}
\seealso{
\code{\link[=somalign_fit_anchored]{somalign_fit_anchored()}}
}
