% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{somalign_plot_match_fraction}
\alias{somalign_plot_match_fraction}
\title{Plot per-node match fraction}
\usage{
somalign_plot_match_fraction(fit, threshold = 0.05)
}
\arguments{
\item{fit}{A \code{somalign_fit} object.}

\item{threshold}{Numeric scalar. Threshold line drawn on the plot.
Default \code{0.05}.}
}
\value{
A \code{ggplot} object.
}
\description{
Sorted bar chart of the match fraction for each query SOM node.
Nodes below \code{threshold} received too little mass from the OT plan and
are the primary candidates for inspection.
}
\examples{
set.seed(1)
mat <- matrix(rnorm(20), nrow = 10, ncol = 2,
              dimnames = list(NULL, c("F1", "F2")))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
qry <- somalign_query(mat, ref, grid = kohonen::somgrid(2, 2, "hexagonal"),
                      rlen = 5)
fit <- somalign_fit(qry, ref)
somalign_plot_match_fraction(fit)
}
