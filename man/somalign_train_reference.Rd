% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference.R
\name{somalign_train_reference}
\alias{somalign_train_reference}
\title{Train a reference SOM and build a somalign reference}
\usage{
somalign_train_reference(
  data,
  labels = NULL,
  features = NULL,
  grid = NULL,
  rlen = 100,
  alpha = c(0.05, 0.01),
  ...
)
}
\arguments{
\item{data}{Numeric matrix or data frame containing old/reference samples.}

\item{labels}{Optional labels, one per row of \code{data}.}

\item{features}{Optional feature names to use. Defaults to all columns.}

\item{grid}{Optional \code{kohonen::somgrid()} object.}

\item{rlen}{Number of SOM training iterations passed to \code{\link[kohonen:som]{kohonen::som()}}.}

\item{alpha}{Learning-rate schedule passed to \code{\link[kohonen:som]{kohonen::som()}}.}

\item{...}{Additional arguments passed to \code{\link[kohonen:som]{kohonen::som()}}.}
}
\value{
A \code{somalign_reference} object.
}
\description{
Train a reference SOM and build a somalign reference
}
\examples{
set.seed(1)
mat <- matrix(rnorm(20), nrow = 10, ncol = 2,
              dimnames = list(NULL, c("F1", "F2")))
labels <- rep(c("A", "B"), each = 5)
ref <- somalign_train_reference(mat, labels = labels,
                                grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
}
