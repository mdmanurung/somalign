% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extend.R
\name{somalign_extend_reference}
\alias{somalign_extend_reference}
\title{Extend an existing reference with new SOM nodes}
\usage{
somalign_extend_reference(
  reference,
  new_codebook,
  new_labels = NULL,
  new_node_masses = NULL,
  new_distance_quantiles = NULL,
  ...
)
}
\arguments{
\item{reference}{A \code{somalign_reference} object to extend.}

\item{new_codebook}{A numeric matrix with one row per new node and one column
per feature.  Column names must match (a superset of) \code{reference$features}
and will be reordered accordingly.}

\item{new_labels}{Optional label specification for the new nodes.  Either:
\describe{
\item{character vector}{Length \code{nrow(new_codebook)}.  Each element is the
class name for that node; the row is one-hot encoded.}
\item{matrix}{\code{nrow(new_codebook)} rows and named columns giving soft
label probabilities.  Rows need not sum to 1; they will be
row-normalised.}
\item{\code{NULL}}{New nodes receive a uniform probability row over all
existing (and any new) classes, or an empty label matrix when the
reference carried no labels.}
}}

\item{new_node_masses}{Optional non-negative numeric vector of length
\code{nrow(new_codebook)}.  \code{NULL} (default) assigns each new node a mass equal
to the mean existing node mass (see section \emph{Mass strategy} above).}

\item{new_distance_quantiles}{Optional numeric matrix of shape
\code{nrow(new_codebook)} x \code{ncol(reference$distance_quantiles)} with the same
column names.  \code{NULL} (default) uses the existing
\code{global_distance_quantiles} row for every new node.}

\item{...}{Reserved for future arguments.}
}
\value{
A \code{somalign_reference} object with \code{n_original + n_new} rows in
\code{codebook}, \code{node_masses}, \code{label_prob}, \code{distance_quantiles}, and
(if present) \code{node_var}.
}
\description{
Appends new nodes (e.g.\ query-derived "novel" prototypes) to a trained
reference \strong{without retraining}.  The resulting object is a valid
\code{somalign_reference} and can be passed directly to \code{\link[=somalign_fit]{somalign_fit()}}.
}
\section{Codebook space}{

\code{new_codebook} must be in \strong{reference-scaled space} — the same space as
\code{reference$codebook}.  If your new node coordinates come from a raw-data SOM,
scale them first with
\code{scale(new_codebook, center = reference$center, scale = reference$scale)}.
}

\section{Mass strategy and OT marginal}{

Node masses form the reference marginal \code{b} for the unbalanced Sinkhorn
solver.  After combining old and new masses, the full vector is
renormalised to sum to 1 by the constructor (\code{.somalign_normalize_masses()}),
so you may supply raw counts or unnormalised weights.

When \code{new_node_masses} is \code{NULL}, each new node is assigned a mass equal to
the \strong{mean of the existing node masses} (i.e.\ \code{mean(reference$node_masses)}
before renormalisation).  This gives each new node a "typical-node" share:
large enough for the unbalanced OT solver to route meaningful mass into those
columns, yet not so large as to redistribute the existing balance.  Rationale:
the solver's \code{v} update is \code{(b/ktu)^tau_b}; a very small \code{b_j} suppresses
transport into column \code{j} even when the corresponding cost is low, so a
"tiny mass" default would cause new nodes to be ignored at query time.

After renormalisation the original nodes' relative proportions are preserved;
each original node's absolute mass shrinks by the factor
\code{n_orig / (n_orig + n_new)} approximately.
}

\section{Label widening}{

When \code{new_labels} introduces class names not present in \code{reference$label_prob},
the existing label probability matrix is widened with zero columns for the new
classes.  The new node rows receive the probabilities supplied via
\code{new_labels}; if \code{new_labels} is a character vector, each new node receives a
one-hot row for its class.  If \code{new_labels} is a matrix it is used directly
as the probability block for the new nodes (must have the same column names
or be column-compatible via name matching).
}

\section{Distance threshold fallback}{

When \code{new_distance_quantiles} is \code{NULL}, each new node inherits
\code{reference$global_distance_quantiles} as its per-node threshold row.  This is
the same conservative fallback that \code{.somalign_thresholds()} applies to NA
entries at fit time, so the extended reference behaves identically for
"unknown" new nodes.  If \code{reference$global_distance_quantiles} is itself all
NA, new rows are filled with \code{Inf} (no outside-detection for those nodes).
}

\examples{
ref <- somalign_reference_from_nodes(
  codebook = matrix(c(-1, 0, 1, 0), nrow = 2, ncol = 2,
                    dimnames = list(NULL, c("F1", "F2"))),
  features = c("F1", "F2"),
  center   = c(F1 = 0, F2 = 0),
  scale    = c(F1 = 1, F2 = 1),
  label_prob = matrix(c(1, 0, 0, 1), nrow = 2,
                      dimnames = list(NULL, c("A", "B")))
)
new_cb <- matrix(c(2, 0), nrow = 1, dimnames = list(NULL, c("F1", "F2")))
extended <- somalign_extend_reference(ref, new_cb, new_labels = "C")
nrow(extended$codebook)   # 3
}
