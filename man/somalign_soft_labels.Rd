% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/soft.R
\name{somalign_soft_labels}
\alias{somalign_soft_labels}
\title{Soft (probabilistic) label projection for query cells}
\usage{
somalign_soft_labels(
  fit,
  node_groups = NULL,
  k = 8L,
  bandwidth = NULL,
  chunk_size = 10000L
)
}
\arguments{
\item{fit}{A \code{somalign_fit} object.}

\item{node_groups}{Optional node-level grouping to project onto instead of the
reference labels. Either a length-\code{n_nodes} vector (one group per reference
node, e.g. a node-to-metacluster map; converted to indicators) or an
\code{n_nodes} by \code{n_groups} matrix of node-group memberships. When \code{NULL}
(default), \code{fit$reference$label_prob} is used, and the reference must carry
labels.}

\item{k}{Integer. Number of nearest reference nodes used for the kernel,
clamped to the number of reference nodes. Default \code{8L}.}

\item{bandwidth}{Positive scalar or \code{NULL}. Gaussian kernel bandwidth in
reference-scaled space. \code{NULL} (default) uses the median nearest-neighbour
distance of the reference codebook.}

\item{chunk_size}{Positive integer. Cells are processed in blocks of this size
to bound peak memory. Default \code{10000L}.}
}
\value{
A numeric matrix of class \code{c("somalign_soft_labels", "matrix")}, one
row per query cell and one column per label/group, with rows summing to 1
(a row is all-zero when a cell's nearest nodes carry no label mass). Row
names are the query sample identifiers; attributes \code{k} and \code{bandwidth}
record the settings used.
}
\description{
Projects each query cell onto the reference by a Gaussian kernel over its k
nearest reference SOM nodes, and returns a per-cell probability distribution
over labels (or any node-level grouping). This is the soft analogue of the
hard nearest-node assignment behind \code{old_som_label} in \code{\link[=somalign_results]{somalign_results()}}.
}
\details{
Hard projection assigns each cell to a single nearest node and inherits that
node's label, discarding the cell's position within the node's Voronoi region.
At a label boundary this makes assignment a discontinuous 0/1 decision, so a
small batch shift can flip a cell's label and a boundary cell contributes a
full unit to one label with no hedging. Soft projection instead spreads each
cell over its nearest nodes' labels, which removes that boundary discontinuity
and reduces the quantisation variance in downstream per-sample frequency
estimates. It changes the \emph{frequency estimate}, not the most-likely label:
\code{max.col()} of a soft-label matrix typically matches the hard label.
}
\section{Coverage contract}{

Soft projection applies \strong{no} acceptance or out-of-reference gating. Unlike
the hard path in \code{\link[=somalign_results]{somalign_results()}} (which flags cells via
\code{outside_reference_distance} / \code{transferred_label_accepted} and can suppress
low-confidence transfers), every cell contributes to the soft distribution
according to its distance to the reference nodes alone. Cells that fall
outside the reference are still projected onto their nearest nodes. If you
need to exclude out-of-reference cells, filter them with \code{\link[=somalign_results]{somalign_results()}}
before aggregating, or subset the query.
}

\examples{
if (requireNamespace("kohonen", quietly = TRUE)) {
  set.seed(1)
  x <- rbind(matrix(rnorm(90 * 3, -3, 0.5), ncol = 3),
             matrix(rnorm(90 * 3,  3, 0.5), ncol = 3))
  colnames(x) <- paste0("m", seq_len(3))
  lab <- rep(c("low", "high"), each = 90)
  grid <- kohonen::somgrid(3, 3, "hexagonal")
  ref <- somalign_train_reference(x, labels = lab, grid = grid, rlen = 10)
  qry <- somalign_query(x, ref, grid = grid, rlen = 10)
  fit <- somalign_fit(qry, ref)
  soft <- somalign_soft_labels(fit)
  head(soft)
}
}
\seealso{
\code{\link[=somalign_soft_frequencies]{somalign_soft_frequencies()}}, \code{\link[=somalign_results]{somalign_results()}}
}
