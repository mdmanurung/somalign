% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{somalign_plot_codebook_range}
\alias{somalign_plot_codebook_range}
\title{Plot query vs reference SOM code ranges per marker}
\usage{
somalign_plot_codebook_range(check)
}
\arguments{
\item{check}{A \code{somalign_codebook_check} object, as returned by
\code{\link[=somalign_check_codebook_alignment]{somalign_check_codebook_alignment()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualises the min-to-max range of each marker's SOM codes for both the
query and reference codebooks. Colours indicate the overlap flag computed
by \code{\link[=somalign_check_codebook_alignment]{somalign_check_codebook_alignment()}}: \code{ok} (green), \code{warning} (orange),
or \code{critical} (red).
}
\examples{
set.seed(1)
mat <- matrix(rnorm(20), nrow = 10, ncol = 2,
              dimnames = list(NULL, c("F1", "F2")))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
qry <- somalign_query(mat, ref, grid = kohonen::somgrid(2, 2, "hexagonal"),
                      rlen = 5)
chk <- somalign_check_codebook_alignment(qry$codebook, ref,
                                         stop_if_critical = FALSE)
somalign_plot_codebook_range(chk)
}
