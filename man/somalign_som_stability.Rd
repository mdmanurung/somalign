% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{somalign_som_stability}
\alias{somalign_som_stability}
\title{Assess alignment stability across query SOM random seeds}
\usage{
somalign_som_stability(
  query_data,
  reference,
  som_seeds = seq_len(5L),
  epsilon = 0.5,
  rho_query = 1,
  rho_ref = 1,
  grid = NULL,
  rlen = 100,
  alpha = c(0.05, 0.01),
  parallel = FALSE,
  ...
)
}
\arguments{
\item{query_data}{Numeric query data.}

\item{reference}{A \code{somalign_reference} object.}

\item{som_seeds}{Integer vector of random seeds used to train query SOMs.}

\item{epsilon}{Entropic regularisation passed to \code{\link[=somalign_fit]{somalign_fit()}}.}

\item{rho_query, rho_ref}{Mass-relaxation parameters passed to \code{\link[=somalign_fit]{somalign_fit()}}.}

\item{grid}{Optional \code{kohonen::somgrid()} for query SOM training.}

\item{rlen}{Number of SOM training iterations.}

\item{alpha}{Learning-rate schedule.}

\item{parallel}{Logical. Use \code{\link[BiocParallel:bplapply]{BiocParallel::bplapply()}} when \code{TRUE}.}

\item{...}{Additional arguments passed to \code{\link[=somalign_query]{somalign_query()}}.}
}
\value{
A data frame with one row per seed containing key alignment summary
statistics: \code{som_seed}, \code{transport_mass}, \code{mean_match_fraction},
\code{max_row_mass_error}, \code{accepted_label_fraction},
\code{outside_direct_fraction}, \code{outside_corrected_fraction},
\code{mean_correction_norm}, \code{converged}.
}
\description{
Trains a new query SOM for each seed and runs the full alignment pipeline,
holding the reference SOM fixed. The returned summary quantifies how much OT
alignment statistics vary with query SOM training randomness — the largest
uncontrolled variance source in the \code{somalign} workflow.
}
\examples{
set.seed(1)
mat <- matrix(rnorm(40), nrow = 20, ncol = 2,
              dimnames = list(NULL, c("F1", "F2")))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
somalign_som_stability(mat, ref, som_seeds = 1:3,
                       grid = kohonen::somgrid(2, 2, "hexagonal"), rlen = 5)
}
