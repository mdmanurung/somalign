% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gromov.R
\name{somalign_fit_gw}
\alias{somalign_fit_gw}
\title{Cross-panel SOM alignment by Gromov-Wasserstein optimal transport (prototype)}
\usage{
somalign_fit_gw(query, reference, epsilon = 0.05, max_iter = 50L, tol = 1e-06)
}
\arguments{
\item{query, reference}{\code{somalign_query} / \code{somalign_reference} objects (only
their \verb{$codebook} and \verb{$node_masses} are used; the codebooks need not share
markers or dimension).}

\item{epsilon}{Entropic regularisation strength for the GW solve. Default \code{0.05}.}

\item{max_iter, tol}{Outer GW iteration budget and convergence tolerance.}
}
\value{
An object of class \code{somalign_gw_fit}: \code{coupling} (query-node by
reference-node transport plan), \code{correspondence} (row-stochastic coupling),
\code{converged}, \code{iterations}, and, when the reference carries labels,
\code{transferred_label} per query node from \code{correspondence \%*\% reference$label_prob}.
}
\description{
Aligns a query SOM to a fixed reference SOM by matching their intra-codebook
distance structures with entropic Gromov-Wasserstein (GW) optimal transport,
so \strong{no shared marker space is required}. This is the route to reusing a
reference across different panels or instruments, which the coordinate-matched
\code{\link[=somalign_fit]{somalign_fit()}} cannot do.
}
\section{Prototype status}{

This implements \emph{balanced} GW, which transports all query mass and therefore
assumes the query and reference share roughly the same populations. Panels that
add or drop populations require the unbalanced/partial GW relaxations of Pamona
(\doi{10.1093/bioinformatics/btab594}) and SCOTv2 (\doi{10.1089/cmb.2022.0270});
those are the intended next step. Treat the correspondence as experimental.
}

\examples{
# (illustrative) align a reference codebook to a rotated copy of itself
set.seed(1)
cb <- matrix(rnorm(18), 6, 3)
rot <- qr.Q(qr(matrix(rnorm(9), 3, 3)))
ref <- structure(list(codebook = cb, node_masses = rep(1/6, 6)),
                 class = "somalign_reference")
qry <- structure(list(codebook = cb \%*\% rot, node_masses = rep(1/6, 6)),
                 class = "somalign_query")
fit <- somalign_fit_gw(qry, ref)
}
