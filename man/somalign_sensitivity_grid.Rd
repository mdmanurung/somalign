% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{somalign_sensitivity_grid}
\alias{somalign_sensitivity_grid}
\title{Run an OT sensitivity grid}
\usage{
somalign_sensitivity_grid(
  query,
  reference,
  epsilon,
  rho_query,
  rho_ref,
  solver = c("internal", "auto", "log_domain", "annealing"),
  min_match_fraction = 0.05,
  confidence_threshold = 0.6,
  correction_min_mass = 1e-08,
  max_iter = 1000,
  tol = 1e-07,
  chunk_size = 10000L,
  diagonal_boost = 0,
  parallel = FALSE,
  anneal_start = 10,
  anneal_stages = 10L,
  anneal_factor = NULL
)
}
\arguments{
\item{query}{A \code{somalign_query} object.}

\item{reference}{A \code{somalign_reference} object.}

\item{epsilon}{Numeric vector of entropic regularisation values.}

\item{rho_query}{Numeric vector of query-side mass relaxation values.}

\item{rho_ref}{Numeric vector of reference-side mass relaxation values.}

\item{solver}{Solver passed to \code{somalign_fit()}. \code{"auto"} is accepted as a
compatibility alias for the internal pure-R solver.}

\item{min_match_fraction}{Minimum match fraction threshold passed to each
\code{somalign_fit()} call. Default \code{0.05}.}

\item{confidence_threshold}{Minimum label confidence for accepted label
transfer. Default \code{0.6}.}

\item{correction_min_mass}{Minimum OT mass for a node shift to be applied.
Default \code{1e-8}.}

\item{max_iter}{Maximum Sinkhorn iterations. Default \code{1000}.}

\item{tol}{Sinkhorn convergence tolerance. Default \code{1e-7}.}

\item{chunk_size}{Integer. Number of samples per projection chunk.
\code{NULL} processes all samples at once. Default \code{10000L}.}

\item{diagonal_boost}{Non-negative scalar added to same-node OT costs to
discourage self-transport. Default \code{0}.}

\item{parallel}{Logical. When \code{TRUE}, grid rows are evaluated in parallel
using \code{\link[BiocParallel:bplapply]{BiocParallel::bplapply()}} with the registered
\code{BiocParallel} back-end (see \code{\link[BiocParallel:register]{BiocParallel::register()}}). Configure the
back-end before calling this function, e.g.
\code{BiocParallel::register(BiocParallel::MulticoreParam(workers = 4))}. When
\code{FALSE} (default) a sequential for-loop is used, which is fully
reproducible across platforms.}

\item{anneal_start, anneal_stages, anneal_factor}{Annealing-schedule tuning
parameters, used only when \code{solver = "annealing"}. See \code{\link[=somalign_fit]{somalign_fit()}}.}
}
\value{
A data frame with one row per parameter combination, including a
\code{mutual_information} column (bits; \code{diagnostics$ot$mutual_information}
for that grid point's fit).
}
\description{
Run an OT sensitivity grid
}
\examples{
set.seed(1)
mat <- matrix(rnorm(20), nrow = 10, ncol = 2,
              dimnames = list(NULL, c("F1", "F2")))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
qry <- somalign_query(mat, ref, grid = kohonen::somgrid(2, 2, "hexagonal"),
                      rlen = 5)
somalign_sensitivity_grid(qry, ref,
                          epsilon = c(0.05, 0.1),
                          rho_query = c(0.5, 1),
                          rho_ref = 1)
}
