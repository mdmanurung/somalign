% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{somalign_fit_two_pass}
\alias{somalign_fit_two_pass}
\title{Two-pass alignment decomposing correction into global and local components}
\usage{
somalign_fit_two_pass(
  query,
  reference,
  epsilon_global = 0.3,
  epsilon_local = 0.1,
  rho_query = 1,
  rho_ref = 1,
  solver = c("internal", "log_domain", "auto"),
  min_match_fraction = 0.05,
  confidence_threshold = 0.6,
  correction_min_mass = 1e-08,
  max_iter = 1000,
  tol = 1e-07,
  chunk_size = 10000L,
  label_guided = FALSE
)
}
\arguments{
\item{query}{A \code{somalign_query} object.}

\item{reference}{A \code{somalign_reference} object.}

\item{epsilon_global}{Entropic regularisation for pass 1 (global). Higher
values give a smoother, more diffuse transport plan that captures the
mean batch shift while averaging out population-specific noise.
Default \code{0.3}; should be larger than \code{epsilon_local}.}

\item{epsilon_local}{Entropic regularisation for pass 2 (local). Should
be smaller than \code{epsilon_global} to refine residual node-level
corrections. Default \code{0.1}.}

\item{rho_query}{Query-side unbalanced mass relaxation (both passes).}

\item{rho_ref}{Reference-side unbalanced mass relaxation (both passes).}

\item{solver}{Sinkhorn solver variant. See \code{\link[=somalign_fit]{somalign_fit()}}.}

\item{min_match_fraction}{Minimum transported fraction for corrections
and label transfer. See \code{\link[=somalign_fit]{somalign_fit()}}.}

\item{confidence_threshold}{Minimum label confidence for transfer
acceptance. See \code{\link[=somalign_fit]{somalign_fit()}}.}

\item{correction_min_mass}{Minimum transported mass for correction.
See \code{\link[=somalign_fit]{somalign_fit()}}.}

\item{max_iter}{Maximum Sinkhorn iterations per pass.}

\item{tol}{Sinkhorn convergence tolerance.}

\item{chunk_size}{Integer. Number of samples per projection chunk.
See \code{\link[=somalign_fit]{somalign_fit()}}.}

\item{label_guided}{Logical. When \code{TRUE}, applies a large cost penalty to
node pairs with discordant dominant labels in both OT passes. See
\code{\link[=somalign_fit]{somalign_fit()}} for details.}
}
\value{
A \code{somalign_fit} object with an additional \verb{$two_pass} list
containing \code{global_shift} (per-feature vector), \code{global_shift_norm}
(Euclidean magnitude), \code{epsilon_global}, and \code{epsilon_local}.
}
\description{
A two-stage variant of \code{\link[=somalign_fit]{somalign_fit()}} that separates the batch correction
into a global shift (estimated at high regularisation) and a local residual
(refined at lower regularisation). This decomposition is most useful when
the batch effect has both a large uniform component and smaller
population-specific residuals.
}
\details{
Pass 1 runs OT at \code{epsilon_global} between the original query codebook and
the reference codebook. The mass-weighted mean node shift across
correction-allowed nodes becomes the global shift \code{g}. Pass 2 runs OT at
\code{epsilon_local} between the globally shifted query codebook and the
reference, capturing residual population-specific displacements. The final
per-node correction is the residual plus \code{g}, so the total correction for
each cell equals its pass-2 barycentric target minus its original codebook
centroid.

Direct projection (\code{old_som_unit}, \code{old_som_label}, \code{final_status}) is
computed from the original unshifted \code{query$scaled_data} and is unaffected
by the transport, preserving the transport-free primary result that
\code{\link[=somalign_fit]{somalign_fit()}} guarantees.

When the batch shift is predominantly global, \code{fit$two_pass$global_shift}
approximates the per-feature batch offset. When the shift is negligible,
the two-pass result converges toward a plain \code{somalign_fit()} at
\code{epsilon_local}.
}
\examples{
set.seed(1)
mat <- matrix(rnorm(40), nrow = 20, ncol = 2,
              dimnames = list(NULL, c("F1", "F2")))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
qry <- somalign_query(mat + 0.5, ref, grid = kohonen::somgrid(2, 2, "hexagonal"),
                      rlen = 5)
fit2 <- somalign_fit_two_pass(qry, ref)
}
\seealso{
\code{\link[=somalign_fit]{somalign_fit()}}, \code{\link[=somalign_normalize]{somalign_normalize()}}
}
