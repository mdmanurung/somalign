% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topology.R
\name{somalign_topology_audit}
\alias{somalign_topology_audit}
\title{Compute a persistent-homology topology audit for a somalign fit}
\usage{
somalign_topology_audit(
  fit,
  threshold = NULL,
  use_tda = FALSE,
  nodes = c("correction_allowed", "all")
)
}
\arguments{
\item{fit}{A \code{somalign_fit} object.}

\item{threshold}{Numeric scalar in reference-scaled Euclidean units. H0
components with persistence (death - birth) greater than this value are
counted as robustly separated populations. \code{NULL} (default) derives the
threshold from the median of \code{reference$distance_quantiles}'s 95th
percentile column -- the reference's own within-population distance
spread.}

\item{use_tda}{Logical. When \code{TRUE}, additionally compute H0 + H1 via
\code{TDA::ripsDiag()} if the \code{TDA} package is installed (falls back silently
to the base-R H0-only result, with a one-time message, when \code{TDA} is
absent). Default \code{FALSE}.}

\item{nodes}{One of \code{"correction_allowed"} (default; recommended -- nodes
with no correction contribute unchanged, potentially spurious topology)
or \code{"all"}. Note that \code{somalign_epsilon_sweep(..., topology = TRUE)}
reports its topology columns using \code{"all"} (the correction-allowed set is
epsilon-dependent, so it cannot be held fixed across a sweep); pass
\code{nodes = "all"} here to reproduce those numbers for a given fit.}
}
\value{
A list of class \code{somalign_topology}; see the source fields
\code{threshold}, \code{threshold_source} (\code{"auto"}/\code{"user"}),
\code{n_components_query}, \code{n_components_corrected}, \code{n_components_reference},
\code{topology_delta} (corrected minus query; negative means merging),
\code{diagram_query}/\code{diagram_corrected}/\code{diagram_reference} (data frames of
birth/death/persistence), \code{bottleneck_h0} and \verb{tda_*} (\code{NULL} unless
\code{use_tda = TRUE} and \code{TDA} is installed), and \code{topology_warning}
(\code{TRUE} when \code{topology_delta != 0}; a warning is also emitted).
}
\description{
Computes the H0 (connected-component) persistence diagram of the query,
corrected-query, and reference codebooks and reports how many robustly
separated populations survive at a given distance threshold. Batch
correction that merges or erases a population shows up as a drop in the
number of H0 components between the query and corrected codebooks.
}
\details{
This is a pure diagnostic: it reads \code{fit} and returns a new object without
modifying it. It is not run automatically inside \code{\link[=somalign_fit]{somalign_fit()}} (too
expensive to compute by default); call it directly or via
\code{somalign_diagnostics(fit, topology = TRUE)}.

High-dimensional marker spaces (p > 20) are valid but nearest-neighbour
distances concentrate, which can make H0 components less visually
intuitive -- a known property of topological data analysis in high
dimensions, not a bug.
}
\section{Role}{
 This is a \strong{diagnostic on the correction path}, answering
"did the barycentric correction merge distinct populations?" -- i.e. is
the \emph{corrected codebook} trustworthy as batch-corrected coordinates. It
does \strong{not} bear on label transfer, which is computed from the transport
plan and never uses \code{node_shifts}. Treat a large negative \code{topology_delta}
as a reason not to use the corrected coordinates for downstream
re-clustering, not as a problem with the transferred labels.
}

\examples{
set.seed(1)
mat <- matrix(rnorm(40), nrow = 20, ncol = 2,
              dimnames = list(NULL, c("F1", "F2")))
ref <- somalign_train_reference(mat, grid = kohonen::somgrid(2, 2, "hexagonal"),
                                rlen = 5)
qry <- somalign_query(mat + 0.5, ref, grid = kohonen::somgrid(2, 2, "hexagonal"),
                      rlen = 5)
fit <- somalign_fit(qry, ref)
somalign_topology_audit(fit)
}
\seealso{
\code{\link[=somalign_diagnostics]{somalign_diagnostics()}}, \code{\link[=somalign_fit]{somalign_fit()}}
}
